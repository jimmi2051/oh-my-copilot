---
applyTo: "**/*"
---

Compatibility profile:

- Interpret OMC-style intents like `autopilot`, `team`, `ralph`, `ultrawork`, and `ultraqa`.
- If task scope is ambiguous, ask clarifying questions before destructive edits.
- Keep responses implementation-oriented and persist key artifacts under `.omc/`.
