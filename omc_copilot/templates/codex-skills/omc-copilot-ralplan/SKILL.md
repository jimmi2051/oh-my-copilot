---
name: omc.ralplan
description: Use when the user requests OMC ralplan compatibility behavior in Codex; this maps to Ralph mode.
---

# OMC Ralplan

Run the compatibility route through Codex:

```bash
omc-copilot run "<task>" --mode ralplan --runtime codex --cwd <path>
```

Treat this as the OMC ralplan alias and report the resolved run output.
