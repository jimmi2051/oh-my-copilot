---
name: omc.session.search
description: Use when the user wants to search persisted OMC session history from Codex.
---

# OMC Session Search

Run:

```bash
omc-copilot session search "<query>" --project-root <path>
```

Searches `.omc/state/omc-copilot-history.jsonl`; report matching history lines concisely.
