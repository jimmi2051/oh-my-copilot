Thanks for contributing! Please follow these steps:

1. Fork the repository and create a feature branch.
2. Run tests locally: python -m pip install -e .[dev] && pytest -q
3. Run pre-commit hooks: pre-commit run --all-files
4. Open a PR against main and include a clear description of the change.

Keep commits small and focused. All changes should include tests where applicable.
